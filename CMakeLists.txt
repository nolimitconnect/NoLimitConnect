
cmake_minimum_required(VERSION 3.22.1)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_INCLUDE_CURRENT_DIR ON)

#=== application version and info ===#
set(PROJECT_MAINTAINER bjones.engineer@gmail.com)
set(PROJECT_WEBSITE_URL nolimitconnect.com)
set(PROJECT_VENDOR nolimitconnect.com)
set(NLC_NAME "nolimitconnect")
set(NLC_VERSION "1.0.1")
set(NLC_VERSION_MAJOR "1")
set(NLC_VERSION_MINOR "0")
set(NLC_VERSION_PATCH "1")

set(CMAKE_CONFIGURATION_TYPES "Release;Debug" CACHE STRING "" FORCE)

if (CMAKE_BUILD_TYPE MATCHES Debug)
    add_definitions(-DDEBUG)
endif()


# print command line parameters
message(STATUS "CMakeArgs: BEGIN")
get_cmake_property(CacheVars CACHE_VARIABLES)
foreach(CacheVar ${CacheVars})
  get_property(CacheVarHelpString CACHE ${CacheVar} PROPERTY HELPSTRING)
  if(CacheVarHelpString STREQUAL "No help, variable specified on the command line.")
    get_property(CacheVarType CACHE ${CacheVar} PROPERTY TYPE)
    if(CacheVarType STREQUAL "UNINITIALIZED")
      set(CacheVarType)
    else()
      set(CacheVarType :${CacheVarType})
    endif()
    set(CMakeArgs "${CMakeArgs} -D${CacheVar}${CacheVarType}=\"${${CacheVar}}\"")
  endif()
endforeach()
message("CMakeArgs: ${CMakeArgs}")
message(STATUS "CMakeArgs: END")

#=== set root directories ===#
set(PROJECT_SRC_ROOT_DIR ${CMAKE_CURRENT_LIST_DIR})
file(MAKE_DIRECTORY "${PROJECT_SRC_ROOT_DIR}/package")
set(PROJECT_PACKAGE_ROOT_DIR "${PROJECT_SRC_ROOT_DIR}/package")

if(DEFINED FLATPAKBUILD)
    message(STATUS "Flatpak Build Target OS Linux")

    project(nolimitconnect LANGUAGES C CXX ASM)

    set(FLATPAKBUILD true)

    set(TARGET_OS_LINUX true)
    add_compile_definitions(TARGET_OS_LINUX=1)
    add_compile_definitions(TARGET_POSIX=1)
    set(TARGET_CPU_64BIT true)
    add_compile_definitions(TARGET_CPU_64BIT=1)
    set(TARGET_CPU_X86_64 true)
    add_compile_definitions(TARGET_CPU_X86_64=1)
    set(ARCH_X86_64 true)
    add_compile_definitions(ARCH_X86_64=1)
    set(ARCH_32_BITS false)
    add_compile_definitions(ARCH_32_BITS=0)
    set(ARCH_64_BITS true)
    add_compile_definitions(ARCH_64_BITS=1)
    add_compile_definitions(NLC_ARCH_LITTLE_ENDIAN=1)
    add_compile_definitions(HAVE_ALIGNED_STACK=1)
    
    if(DEFINED USE_CMAKE_DEFINES_INSTEAD_OF_DASH_P_FOR_NASM)
        set(HAVE_AVX true)
        set(HAVE_I686 true)
        set(HAVE_MMX true)
        set(HAVE_MMXEXT true)
        set(HAVE_SSE true)
        set(HAVE_SSE2 true)

        #it seems the nasm -P does not work in flatpak so add all the defines here for ffmpeg etc.
        add_compile_definitions(ARCH_AARCH64=0)
        add_compile_definitions(ARCH_ALPHA=0)
        add_compile_definitions(ARCH_ARM=0)
        add_compile_definitions(ARCH_AVR32=0)
        add_compile_definitions(ARCH_AVR32_AP=0)
        add_compile_definitions(ARCH_AVR32_UC=0)
        add_compile_definitions(ARCH_BFIN=0)
        add_compile_definitions(ARCH_IA64=0)
        add_compile_definitions(ARCH_LOONGARCH=0)
        add_compile_definitions(ARCH_LOONGARCH32=0)
        add_compile_definitions(ARCH_LOONGARCH6=0)
        add_compile_definitions(ARCH_M68K=0)
        add_compile_definitions(ARCH_MIPS=0)
        add_compile_definitions(ARCH_MIPS64=0)
        add_compile_definitions(ARCH_PARISC=0)
        add_compile_definitions(ARCH_PPC=0)
        add_compile_definitions(ARCH_PPC64=0)
        add_compile_definitions(ARCH_RISCV=0)
        add_compile_definitions(ARCH_S390=0)
        add_compile_definitions(ARCH_SH4=0)
        add_compile_definitions(ARCH_SPARC=0)
        add_compile_definitions(ARCH_SPARC64=0)
        add_compile_definitions(ARCH_TILEGX=0)
        add_compile_definitions(ARCH_TILEPRO=0)
        add_compile_definitions(ARCH_TOMI=0)
        add_compile_definitions(ARCH_X86=1)
        add_compile_definitions(ARCH_X86_32=0)
        add_compile_definitions(ARCH_X86_64=1)
        add_compile_definitions(HAVE_ARMV5TE=0)
        add_compile_definitions(HAVE_ARMV6=0)
        add_compile_definitions(HAVE_ARMV6T2=0)
        add_compile_definitions(HAVE_ARMV8=0)
        add_compile_definitions(HAVE_NEON=0)
        add_compile_definitions(HAVE_VFP=0)
        add_compile_definitions(HAVE_VFPV3=0)
        add_compile_definitions(HAVE_SETEND=0)
        add_compile_definitions(HAVE_ALTIVEC=0)
        add_compile_definitions(HAVE_DCBZL=0)
        add_compile_definitions(HAVE_LDBRX=0)
        add_compile_definitions(HAVE_POWER8=0)
        add_compile_definitions(HAVE_PPC4XX=0)
        add_compile_definitions(HAVE_VSX=0)
        add_compile_definitions(HAVE_RVV=0)
        add_compile_definitions(HAVE_AESNI=1)
        add_compile_definitions(HAVE_AMD3DNOW=0)
        add_compile_definitions(HAVE_AMD3DNOWEXT=0)
        add_compile_definitions(HAVE_AVX=1)
        add_compile_definitions(HAVE_AVX2=1)
        add_compile_definitions(HAVE_AVX512=1)
        add_compile_definitions(HAVE_AVX512ICL=0)
        add_compile_definitions(HAVE_FMA3=1)
        add_compile_definitions(HAVE_FMA4=1)
        add_compile_definitions(HAVE_MMX=1)
        add_compile_definitions(HAVE_MMXEXT=1)
        add_compile_definitions(HAVE_SSE=1)
        add_compile_definitions(HAVE_SSE2=1)
        add_compile_definitions(HAVE_SSE3=1)
        add_compile_definitions(HAVE_SSE4=1)
        add_compile_definitions(HAVE_SSE42=1)
        add_compile_definitions(HAVE_SSSE3=1)
        add_compile_definitions(HAVE_XOP=1)
        add_compile_definitions(HAVE_CPUNOP=0)
        add_compile_definitions(HAVE_I686=1)
        add_compile_definitions(HAVE_MIPSFPU=0)
        add_compile_definitions(HAVE_MIPS32R2=0)
        add_compile_definitions(HAVE_MIPS32R5=0)
        add_compile_definitions(HAVE_MIPS64R2=0)
        add_compile_definitions(HAVE_MIPS32R6=0)
        add_compile_definitions(HAVE_MIPS64R6=0)
        add_compile_definitions(HAVE_MIPSDSP=0)
        add_compile_definitions(HAVE_MIPSDSPR2=0)
        add_compile_definitions(HAVE_MSA=0)
        add_compile_definitions(HAVE_LOONGSON2=0)
        add_compile_definitions(HAVE_LOONGSON3=0)
        add_compile_definitions(HAVE_MMI=0)
        add_compile_definitions(HAVE_LSX=0)
        add_compile_definitions(HAVE_LASX=0)
        add_compile_definitions(HAVE_ARMV5TE_EXTERNAL=0)
        add_compile_definitions(HAVE_ARMV6_EXTERNAL=0)
        add_compile_definitions(HAVE_ARMV6T2_EXTERNAL=0)
        add_compile_definitions(HAVE_ARMV8_EXTERNAL=0)
        add_compile_definitions(HAVE_NEON_EXTERNAL=0)
        add_compile_definitions(HAVE_VFP_EXTERNAL=0)
        add_compile_definitions(HAVE_VFPV3_EXTERNAL=0)
        add_compile_definitions(HAVE_SETEND_EXTERNAL=0)
        add_compile_definitions(HAVE_ALTIVEC_EXTERNAL=0)
        add_compile_definitions(HAVE_DCBZL_EXTERNAL=0)
        add_compile_definitions(HAVE_LDBRX_EXTERNAL=0)
        add_compile_definitions(HAVE_POWER8_EXTERNAL=0)
        add_compile_definitions(HAVE_PPC4XX_EXTERNAL=0)
        add_compile_definitions(HAVE_VSX_EXTERNAL=0)
        add_compile_definitions(HAVE_RVV_EXTERNAL=0)
        add_compile_definitions(HAVE_AESNI_EXTERNAL=1)
        add_compile_definitions(HAVE_AMD3DNOW_EXTERNAL=0)
        add_compile_definitions(HAVE_AMD3DNOWEXT_EXTERNAL=0)
        add_compile_definitions(HAVE_AVX_EXTERNAL=1)
        add_compile_definitions(HAVE_AVX2_EXTERNAL=1)
        add_compile_definitions(HAVE_AVX512_EXTERNAL=1)
        add_compile_definitions(HAVE_AVX512ICL_EXTERNAL=1)
        add_compile_definitions(HAVE_FMA3_EXTERNAL=1)
        add_compile_definitions(HAVE_FMA4_EXTERNAL=1)
        add_compile_definitions(HAVE_MMX_EXTERNAL=1)
        add_compile_definitions(HAVE_MMXEXT_EXTERNAL=1)
        add_compile_definitions(HAVE_SSE_EXTERNAL=1)
        add_compile_definitions(HAVE_SSE2_EXTERNAL=1)
        add_compile_definitions(HAVE_SSE3_EXTERNAL=1)
        add_compile_definitions(HAVE_SSE4_EXTERNAL=1)
        add_compile_definitions(HAVE_SSE42_EXTERNAL=1)
        add_compile_definitions(HAVE_SSSE3_EXTERNAL=1)
        add_compile_definitions(HAVE_XOP_EXTERNAL=1)
        add_compile_definitions(HAVE_CPUNOP_EXTERNAL=0)
        add_compile_definitions(HAVE_I686_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPSFPU_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPS32R2_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPS32R5_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPS64R2_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPS32R6_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPS64R6_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPSDSP_EXTERNAL=0)
        add_compile_definitions(HAVE_MIPSDSPR2_EXTERNAL=0)
        add_compile_definitions(HAVE_MSA_EXTERNAL=0)
        add_compile_definitions(HAVE_LOONGSON2_EXTERNAL=0)
        add_compile_definitions(HAVE_LOONGSON3_EXTERNAL=0)
        add_compile_definitions(HAVE_MMI_EXTERNAL=0)
        add_compile_definitions(HAVE_LSX_EXTERNAL=0)
        add_compile_definitions(HAVE_LASX_EXTERNAL=0)
        add_compile_definitions(HAVE_ARMV5TE_INLINE=0)
        add_compile_definitions(HAVE_ARMV6_INLINE=0)
        add_compile_definitions(HAVE_ARMV6T2_INLINE=0)
        add_compile_definitions(HAVE_ARMV8_INLINE=0)
        add_compile_definitions(HAVE_NEON_INLINE=0)
        add_compile_definitions(HAVE_VFP_INLINE=0)
        add_compile_definitions(HAVE_VFPV3_INLINE=0)
        add_compile_definitions(HAVE_SETEND_INLINE=0)
        add_compile_definitions(HAVE_ALTIVEC_INLINE=0)
        add_compile_definitions(HAVE_DCBZL_INLINE=0)
        add_compile_definitions(HAVE_LDBRX_INLINE=0)
        add_compile_definitions(HAVE_POWER8_INLINE=0)
        add_compile_definitions(HAVE_PPC4XX_INLINE=0)
        add_compile_definitions(HAVE_VSX_INLINE=0)
        add_compile_definitions(HAVE_RVV_INLINE=0)
        add_compile_definitions(HAVE_AESNI_INLINE=1)
        add_compile_definitions(HAVE_AMD3DNOW_INLINE=0)
        add_compile_definitions(HAVE_AMD3DNOWEXT_INLINE=0)
        add_compile_definitions(HAVE_AVX_INLINE=1)
        add_compile_definitions(HAVE_AVX2_INLINE=1)
        add_compile_definitions(HAVE_AVX512_INLINE=1)
        add_compile_definitions(HAVE_AVX512ICL_INLINE=0)
        add_compile_definitions(HAVE_FMA3_INLINE=1)
        add_compile_definitions(HAVE_FMA4_INLINE=1)
        add_compile_definitions(HAVE_MMX_INLINE=1)
        add_compile_definitions(HAVE_MMXEXT_INLINE=1)
        add_compile_definitions(HAVE_SSE_INLINE=1)
        add_compile_definitions(HAVE_SSE2_INLINE=1)
        add_compile_definitions(HAVE_SSE3_INLINE=1)
        add_compile_definitions(HAVE_SSE4_INLINE=1)
        add_compile_definitions(HAVE_SSE42_INLINE=1)
        add_compile_definitions(HAVE_SSSE3_INLINE=1)
        add_compile_definitions(HAVE_XOP_INLINE=1)
        add_compile_definitions(HAVE_CPUNOP_INLINE=0)
        add_compile_definitions(HAVE_I686_INLINE=0)
        add_compile_definitions(HAVE_MIPSFPU_INLINE=0)
        add_compile_definitions(HAVE_MIPS32R2_INLINE=0)
        add_compile_definitions(HAVE_MIPS32R5_INLINE=0)
        add_compile_definitions(HAVE_MIPS64R2_INLINE=0)
        add_compile_definitions(HAVE_MIPS32R6_INLINE=0)
        add_compile_definitions(HAVE_MIPS64R6_INLINE=0)
        add_compile_definitions(HAVE_MIPSDSP_INLINE=0)
        add_compile_definitions(HAVE_MIPSDSPR2_INLINE=0)
        add_compile_definitions(HAVE_MSA_INLINE=0)
        add_compile_definitions(HAVE_LOONGSON2_INLINE=0)
        add_compile_definitions(HAVE_LOONGSON3_INLINE=0)
        add_compile_definitions(HAVE_MMI_INLINE=0)
        add_compile_definitions(HAVE_LSX_INLINE=0)
        add_compile_definitions(HAVE_LASX_INLINE=0)
        add_compile_definitions(HAVE_ALIGNED_STACK=1)
        add_compile_definitions(HAVE_FAST_64BIT=1)
        add_compile_definitions(HAVE_FAST_CLZ=1)
        add_compile_definitions(HAVE_FAST_CMOV=1)
        add_compile_definitions(HAVE_FAST_FLOAT16=0)
        add_compile_definitions(HAVE_LOCAL_ALIGNED=1)
        add_compile_definitions(HAVE_SIMD_ALIGN_16=1)
        add_compile_definitions(HAVE_SIMD_ALIGN_32=1)
        add_compile_definitions(HAVE_SIMD_ALIGN_64=1)
        add_compile_definitions(HAVE_ATOMIC_CAS_PTR=0)
        add_compile_definitions(HAVE_MACHINE_RW_BARRIER=0)
        add_compile_definitions(HAVE_MEMORYBARRIER=0)
        add_compile_definitions(HAVE_MM_EMPTY=1)
        add_compile_definitions(HAVE_RDTSC=0)
        add_compile_definitions(HAVE_SEM_TIMEDWAIT=1)
        add_compile_definitions(HAVE_SYNC_VAL_COMPARE_AND_SWAP=1)
        add_compile_definitions(HAVE_INLINE_ASM=1)
        add_compile_definitions(HAVE_SYMVER=1)
        add_compile_definitions(HAVE_X86ASM=1)
        add_compile_definitions(HAVE_BIGENDIAN=0)
        add_compile_definitions(HAVE_FAST_UNALIGNED=1)
        add_compile_definitions(HAVE_ARPA_INET_H=1)
        add_compile_definitions(HAVE_ASM_TYPES_H=0)
        add_compile_definitions(HAVE_CDIO_PARANOIA_H=0)
        add_compile_definitions(HAVE_CDIO_PARANOIA_PARANOIA_H=0)
        add_compile_definitions(HAVE_CUDA_H=0)
        add_compile_definitions(HAVE_DISPATCH_DISPATCH_H=0)
        add_compile_definitions(HAVE_DEV_BKTR_IOCTL_BT848_H=0)
        add_compile_definitions(HAVE_DEV_BKTR_IOCTL_METEOR_H=0)
        add_compile_definitions(HAVE_DEV_IC_BT8XX_H=0)
        add_compile_definitions(HAVE_DEV_VIDEO_BKTR_IOCTL_BT848_H=0)
        add_compile_definitions(HAVE_DEV_VIDEO_METEOR_IOCTL_METEOR_H=0)
        add_compile_definitions(HAVE_DIRECT_H=0)
        add_compile_definitions(HAVE_DIRENT_H=1)
        add_compile_definitions(HAVE_DXGIDEBUG_H=0)
        add_compile_definitions(HAVE_DXVA_H=0)
        add_compile_definitions(HAVE_ES2_GL_H=0)
        add_compile_definitions(HAVE_GSM_H=0)
        add_compile_definitions(HAVE_IO_H=0)
        add_compile_definitions(HAVE_LINUX_DMA_BUF_H=0)
        add_compile_definitions(HAVE_LINUX_PERF_EVENT_H=1)
        add_compile_definitions(HAVE_MACHINE_IOCTL_BT848_H=0)
        add_compile_definitions(HAVE_MACHINE_IOCTL_METEOR_H=0)
        add_compile_definitions(HAVE_MALLOC_H=1)
        add_compile_definitions(HAVE_OPENCV2_CORE_CORE_C_H=0)
        add_compile_definitions(HAVE_OPENGL_GL3_H=0)
        add_compile_definitions(HAVE_POLL_H=1)
        add_compile_definitions(HAVE_SYS_PARAM_H=1)
        add_compile_definitions(HAVE_SYS_RESOURCE_H=1)
        add_compile_definitions(HAVE_SYS_SELECT_H=1)
        add_compile_definitions(HAVE_SYS_SOUNDCARD_H=1)
        add_compile_definitions(HAVE_SYS_TIME_H=1)
        add_compile_definitions(HAVE_SYS_UN_H=1)
        add_compile_definitions(HAVE_SYS_VIDEOIO_H=0)
        add_compile_definitions(HAVE_TERMIOS_H=1)
        add_compile_definitions(HAVE_UDPLITE_H=0)
        add_compile_definitions(HAVE_UNISTD_H=1)
        add_compile_definitions(HAVE_VALGRIND_VALGRIND_H=0)
        add_compile_definitions(HAVE_WINDOWS_H=0)
        add_compile_definitions(HAVE_WINSOCK2_H=0)
        add_compile_definitions(HAVE_INTRINSICS_NEON=0)
        add_compile_definitions(HAVE_ATANF=1)
        add_compile_definitions(HAVE_ATAN2F=1)
        add_compile_definitions(HAVE_CBRT=1)
        add_compile_definitions(HAVE_CBRTF=1)
        add_compile_definitions(HAVE_COPYSIGN=1)
        add_compile_definitions(HAVE_COSF=1)
        add_compile_definitions(HAVE_ERF=1)
        add_compile_definitions(HAVE_EXP2=1)
        add_compile_definitions(HAVE_EXP2F=1)
        add_compile_definitions(HAVE_EXPF=1)
        add_compile_definitions(HAVE_HYPOT=1)
        add_compile_definitions(HAVE_ISFINITE=1)
        add_compile_definitions(HAVE_ISINF=1)
        add_compile_definitions(HAVE_ISNAN=1)
        add_compile_definitions(HAVE_LDEXPF=1)
        add_compile_definitions(HAVE_LLRINT=1)
        add_compile_definitions(HAVE_LLRINTF=1)
        add_compile_definitions(HAVE_LOG2=1)
        add_compile_definitions(HAVE_LOG2F=1)
        add_compile_definitions(HAVE_LOG10F=1)
        add_compile_definitions(HAVE_LRINT=1)
        add_compile_definitions(HAVE_LRINTF=1)
        add_compile_definitions(HAVE_POWF=1)
        add_compile_definitions(HAVE_RINT=1)
        add_compile_definitions(HAVE_ROUND=1)
        add_compile_definitions(HAVE_ROUNDF=1)
        add_compile_definitions(HAVE_SINF=1)
        add_compile_definitions(HAVE_TRUNC=1)
        add_compile_definitions(HAVE_TRUNCF=1)
        add_compile_definitions(HAVE_DOS_PATHS=0)
        add_compile_definitions(HAVE_LIBC_MSVCRT=0)
        add_compile_definitions(HAVE_MMAL_PARAMETER_VIDEO_MAX_NUM_CALLBACKS=0)
        add_compile_definitions(HAVE_SECTION_DATA_REL_RO=1)
        add_compile_definitions(HAVE_THREADS=1)
        add_compile_definitions(HAVE_UWP=0)
        add_compile_definitions(HAVE_WINRT=0)
        add_compile_definitions(HAVE_ACCESS=1)
        add_compile_definitions(HAVE_ALIGNED_MALLOC=0)
        add_compile_definitions(HAVE_ARC4RANDOM=0)
        add_compile_definitions(HAVE_CLOCK_GETTIME=1)
        add_compile_definitions(HAVE_CLOSESOCKET=0)
        add_compile_definitions(HAVE_COMMANDLINETOARGVW=0)
        add_compile_definitions(HAVE_FCNTL=1)
        add_compile_definitions(HAVE_GETADDRINFO=1)
        add_compile_definitions(HAVE_GETAUXVAL=1)
        add_compile_definitions(HAVE_GETENV=1)
        add_compile_definitions(HAVE_GETHRTIME=0)
        add_compile_definitions(HAVE_GETOPT=1)
        add_compile_definitions(HAVE_GETMODULEHANDLE=0)
        add_compile_definitions(HAVE_GETPROCESSAFFINITYMASK=0)
        add_compile_definitions(HAVE_GETPROCESSMEMORYINFO=0)
        add_compile_definitions(HAVE_GETPROCESSTIMES=0)
        add_compile_definitions(HAVE_GETRUSAGE=1)
        add_compile_definitions(HAVE_GETSTDHANDLE=0)
        add_compile_definitions(HAVE_GETSYSTEMTIMEASFILETIME=0)
        add_compile_definitions(HAVE_GETTIMEOFDAY=1)
        add_compile_definitions(HAVE_GLOB=1)
        add_compile_definitions(HAVE_GLXGETPROCADDRESS=0)
        add_compile_definitions(HAVE_GMTIME_R=1)
        add_compile_definitions(HAVE_INET_ATON=1)
        add_compile_definitions(HAVE_ISATTY=1)
        add_compile_definitions(HAVE_KBHIT=0)
        add_compile_definitions(HAVE_LOCALTIME_R=1)
        add_compile_definitions(HAVE_LSTAT=1)
        add_compile_definitions(HAVE_LZO1X_999_COMPRESS=0)
        add_compile_definitions(HAVE_MACH_ABSOLUTE_TIME=0)
        add_compile_definitions(HAVE_MAPVIEWOFFILE=0)
        add_compile_definitions(HAVE_MEMALIGN=1)
        add_compile_definitions(HAVE_MKSTEMP=1)
        add_compile_definitions(HAVE_MMAP=1)
        add_compile_definitions(HAVE_MPROTECT=1)
        add_compile_definitions(HAVE_NANOSLEEP=1)
        add_compile_definitions(HAVE_PEEKNAMEDPIPE=0)
        add_compile_definitions(HAVE_POSIX_MEMALIGN=1)
        add_compile_definitions(HAVE_PRCTL=0)
        add_compile_definitions(HAVE_PTHREAD_CANCEL=1)
        add_compile_definitions(HAVE_SCHED_GETAFFINITY=1)
        add_compile_definitions(HAVE_SECITEMIMPORT=0)
        add_compile_definitions(HAVE_SETCONSOLETEXTATTRIBUTE=0)
        add_compile_definitions(HAVE_SETCONSOLECTRLHANDLER=0)
        add_compile_definitions(HAVE_SETDLLDIRECTORY=0)
        add_compile_definitions(HAVE_SETMODE=0)
        add_compile_definitions(HAVE_SETRLIMIT=1)
        add_compile_definitions(HAVE_SLEEP=0)
        add_compile_definitions(HAVE_STRERROR_R=1)
        add_compile_definitions(HAVE_SYSCONF=1)
        add_compile_definitions(HAVE_SYSCTL=0)
        add_compile_definitions(HAVE_USLEEP=1)
        add_compile_definitions(HAVE_UTGETOSTYPEFROMSTRING=0)
        add_compile_definitions(HAVE_VIRTUALALLOC=0)
        add_compile_definitions(HAVE_WGLGETPROCADDRESS=0)
        add_compile_definitions(HAVE_BCRYPT=0)
        add_compile_definitions(HAVE_VAAPI_DRM=1)
        add_compile_definitions(HAVE_VAAPI_X11=1)
        add_compile_definitions(HAVE_VDPAU_X11=1)
        add_compile_definitions(HAVE_PTHREADS=1)
        add_compile_definitions(HAVE_OS2THREADS=0)
        add_compile_definitions(HAVE_W32THREADS=0)
        add_compile_definitions(HAVE_AS_ARCH_DIRECTIVE=0)
        add_compile_definitions(HAVE_AS_DN_DIRECTIVE=0)
        add_compile_definitions(HAVE_AS_FPU_DIRECTIVE=0)
        add_compile_definitions(HAVE_AS_FUNC=0)
        add_compile_definitions(HAVE_AS_OBJECT_ARCH=0)
        add_compile_definitions(HAVE_ASM_MOD_Q=0)
        add_compile_definitions(HAVE_BLOCKS_EXTENSION=0)
        add_compile_definitions(HAVE_EBP_AVAILABLE=1)
        add_compile_definitions(HAVE_EBX_AVAILABLE=1)
        add_compile_definitions(HAVE_GNU_AS=0)
        add_compile_definitions(HAVE_GNU_WINDRES=0)
        add_compile_definitions(HAVE_IBM_ASM=0)
        add_compile_definitions(HAVE_INLINE_ASM_DIRECT_SYMBOL_REFS=1)
        add_compile_definitions(HAVE_INLINE_ASM_LABELS=1)
        add_compile_definitions(HAVE_INLINE_ASM_NONLOCAL_LABELS=1)
        add_compile_definitions(HAVE_PRAGMA_DEPRECATED=0)
        add_compile_definitions(HAVE_RSYNC_CONTIMEOUT=1)
        add_compile_definitions(HAVE_SYMVER_ASM_LABEL=0)
        add_compile_definitions(HAVE_SYMVER_GNU_ASM=1)
        add_compile_definitions(HAVE_VFP_ARGS=0)
        add_compile_definitions(HAVE_XFORM_ASM=0)
        add_compile_definitions(HAVE_XMM_CLOBBERS=1)
        add_compile_definitions(HAVE_DPI_AWARENESS_CONTEXT=0)
        add_compile_definitions(HAVE_IDXGIOUTPUT5=0)
        add_compile_definitions(HAVE_KCMVIDEOCODECTYPE_HEVC=0)
        add_compile_definitions(HAVE_KCMVIDEOCODECTYPE_HEVCWITHALPHA=0)
        add_compile_definitions(HAVE_KCMVIDEOCODECTYPE_VP9=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_420YPCBCR10BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_422YPCBCR8BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_422YPCBCR10BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_422YPCBCR16BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_444YPCBCR8BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_444YPCBCR10BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVPIXELFORMATTYPE_444YPCBCR16BIPLANARVIDEORANGE=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERTRANSFERFUNCTION_SMPTE_ST_2084_PQ=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERTRANSFERFUNCTION_ITU_R_2100_HLG=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERTRANSFERFUNCTION_LINEAR=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERYCBCRMATRIX_ITU_R_2020=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERCOLORPRIMARIES_ITU_R_2020=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERTRANSFERFUNCTION_ITU_R_2020=0)
        add_compile_definitions(HAVE_KCVIMAGEBUFFERTRANSFERFUNCTION_SMPTE_ST_428_1=0)
        add_compile_definitions(HAVE_SOCKLEN_T=1)
        add_compile_definitions(HAVE_STRUCT_ADDRINFO=1)
        add_compile_definitions(HAVE_STRUCT_GROUP_SOURCE_REQ=1)
        add_compile_definitions(HAVE_STRUCT_IP_MREQ_SOURCE=1)
        add_compile_definitions(HAVE_STRUCT_IPV6_MREQ=1)
        add_compile_definitions(HAVE_STRUCT_MSGHDR_MSG_FLAGS=1)
        add_compile_definitions(HAVE_STRUCT_POLLFD=1)
        add_compile_definitions(HAVE_STRUCT_RUSAGE_RU_MAXRSS=1)
        add_compile_definitions(HAVE_STRUCT_SCTP_EVENT_SUBSCRIBE=0)
        add_compile_definitions(HAVE_STRUCT_SOCKADDR_IN6=1)
        add_compile_definitions(HAVE_STRUCT_SOCKADDR_SA_LEN=0)
        add_compile_definitions(HAVE_STRUCT_SOCKADDR_STORAGE=1)
        add_compile_definitions(HAVE_STRUCT_STAT_ST_MTIM_TV_NSEC=1)
        add_compile_definitions(HAVE_STRUCT_V4L2_FRMIVALENUM_DISCRETE=1)
        add_compile_definitions(HAVE_GZIP=1)
        add_compile_definitions(HAVE_LIBDRM_GETFB2=0)
        add_compile_definitions(HAVE_MAKEINFO=1)
        add_compile_definitions(HAVE_MAKEINFO_HTML=1)
        add_compile_definitions(HAVE_OPENCL_D3D11=0)
        add_compile_definitions(HAVE_OPENCL_DRM_ARM=0)
        add_compile_definitions(HAVE_OPENCL_DRM_BEIGNET=0)
        add_compile_definitions(HAVE_OPENCL_DXVA2=0)
        add_compile_definitions(HAVE_OPENCL_VAAPI_BEIGNET=0)
        add_compile_definitions(HAVE_OPENCL_VAAPI_INTEL_MEDIA=0)
        add_compile_definitions(HAVE_PERL=1)
        add_compile_definitions(HAVE_POD2MAN=1)
        add_compile_definitions(HAVE_TEXI2HTML=0)
        add_compile_definitions(HAVE_XMLLINT=0)
        add_compile_definitions(HAVE_ZLIB_GZIP=1)
        add_compile_definitions(CONFIG_DOC=0)
        add_compile_definitions(CONFIG_HTMLPAGES=0)
        add_compile_definitions(CONFIG_MANPAGES=0)
        add_compile_definitions(CONFIG_PODPAGES=0)
        add_compile_definitions(CONFIG_TXTPAGES=0)
        add_compile_definitions(CONFIG_AVIO_HTTP_SERVE_FILES_EXAMPLE=0)
        add_compile_definitions(CONFIG_AVIO_LIST_DIR_EXAMPLE=0)
        add_compile_definitions(CONFIG_AVIO_READ_CALLBACK_EXAMPLE=0)
        add_compile_definitions(CONFIG_DECODE_AUDIO_EXAMPLE=0)
        add_compile_definitions(CONFIG_DECODE_FILTER_AUDIO_EXAMPLE=0)
        add_compile_definitions(CONFIG_DECODE_FILTER_VIDEO_EXAMPLE=0)
        add_compile_definitions(CONFIG_DECODE_VIDEO_EXAMPLE=0)
        add_compile_definitions(CONFIG_DEMUX_DECODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_ENCODE_AUDIO_EXAMPLE=0)
        add_compile_definitions(CONFIG_ENCODE_VIDEO_EXAMPLE=0)
        add_compile_definitions(CONFIG_EXTRACT_MVS_EXAMPLE=0)
        add_compile_definitions(CONFIG_FILTER_AUDIO_EXAMPLE=0)
        add_compile_definitions(CONFIG_HW_DECODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_MUX_EXAMPLE=0)
        add_compile_definitions(CONFIG_QSV_DECODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_REMUX_EXAMPLE=0)
        add_compile_definitions(CONFIG_RESAMPLE_AUDIO_EXAMPLE=0)
        add_compile_definitions(CONFIG_SCALE_VIDEO_EXAMPLE=0)
        add_compile_definitions(CONFIG_SHOW_METADATA_EXAMPLE=0)
        add_compile_definitions(CONFIG_TRANSCODE_AAC_EXAMPLE=0)
        add_compile_definitions(CONFIG_TRANSCODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_VAAPI_ENCODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_VAAPI_TRANSCODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_QSV_TRANSCODE_EXAMPLE=0)
        add_compile_definitions(CONFIG_AVISYNTH=0)
        add_compile_definitions(CONFIG_FREI0R=0)
        add_compile_definitions(CONFIG_LIBCDIO=0)
        add_compile_definitions(CONFIG_LIBDAVS2=0)
        add_compile_definitions(CONFIG_LIBRUBBERBAND=0)
        add_compile_definitions(CONFIG_LIBVIDSTAB=0)
        add_compile_definitions(CONFIG_LIBX264=1)
        add_compile_definitions(CONFIG_LIBX265=1)
        add_compile_definitions(CONFIG_LIBXAVS=0)
        add_compile_definitions(CONFIG_LIBXAVS2=0)
        add_compile_definitions(CONFIG_LIBXVID=0)
        add_compile_definitions(CONFIG_DECKLINK=0)
        add_compile_definitions(CONFIG_LIBFDK_AAC=0)
        add_compile_definitions(CONFIG_LIBTLS=0)
        add_compile_definitions(CONFIG_GMP=0)
        add_compile_definitions(CONFIG_LIBARIBB24=0)
        add_compile_definitions(CONFIG_LIBLENSFUN=0)
        add_compile_definitions(CONFIG_LIBOPENCORE_AMRNB=0)
        add_compile_definitions(CONFIG_LIBOPENCORE_AMRWB=0)
        add_compile_definitions(CONFIG_LIBVO_AMRWBENC=0)
        add_compile_definitions(CONFIG_MBEDTLS=0)
        add_compile_definitions(CONFIG_RKMPP=0)
        add_compile_definitions(CONFIG_LIBSMBCLIENT=0)
        add_compile_definitions(CONFIG_CHROMAPRINT=0)
        add_compile_definitions(CONFIG_GCRYPT=0)
        add_compile_definitions(CONFIG_GNUTLS=1)
        add_compile_definitions(CONFIG_JNI=0)
        add_compile_definitions(CONFIG_LADSPA=0)
        add_compile_definitions(CONFIG_LCMS2=0)
        add_compile_definitions(CONFIG_LIBAOM=0)
        add_compile_definitions(CONFIG_LIBASS=1)
        add_compile_definitions(CONFIG_LIBBLURAY=0)
        add_compile_definitions(CONFIG_LIBBS2B=0)
        add_compile_definitions(CONFIG_LIBCACA=0)
        add_compile_definitions(CONFIG_LIBCELT=0)
        add_compile_definitions(CONFIG_LIBCODEC2=0)
        add_compile_definitions(CONFIG_LIBDAV1D=1)
        add_compile_definitions(CONFIG_LIBDC1394=0)
        add_compile_definitions(CONFIG_LIBDRM=0)
        add_compile_definitions(CONFIG_LIBFLITE=0)
        add_compile_definitions(CONFIG_LIBFONTCONFIG=0)
        add_compile_definitions(CONFIG_LIBFREETYPE=1)
        add_compile_definitions(CONFIG_LIBFRIBIDI=0)
        add_compile_definitions(CONFIG_LIBGLSLANG=0)
        add_compile_definitions(CONFIG_LIBGME=0)
        add_compile_definitions(CONFIG_LIBGSM=0)
        add_compile_definitions(CONFIG_LIBIEC61883=0)
        add_compile_definitions(CONFIG_LIBILBC=0)
        add_compile_definitions(CONFIG_LIBJACK=0)
        add_compile_definitions(CONFIG_LIBJXL=0)
        add_compile_definitions(CONFIG_LIBKLVANC=0)
        add_compile_definitions(CONFIG_LIBKVAZAAR=0)
        add_compile_definitions(CONFIG_LIBMODPLUG=0)
        add_compile_definitions(CONFIG_LIBMP3LAME=1)
        add_compile_definitions(CONFIG_LIBMYSOFA=0)
        add_compile_definitions(CONFIG_LIBOPENCV=0)
        add_compile_definitions(CONFIG_LIBOPENH264=0)
        add_compile_definitions(CONFIG_LIBOPENJPEG=0)
        add_compile_definitions(CONFIG_LIBOPENMPT=0)
        add_compile_definitions(CONFIG_LIBOPENVINO=0)
        add_compile_definitions(CONFIG_LIBOPUS=1)
        add_compile_definitions(CONFIG_LIBPLACEBO=0)
        add_compile_definitions(CONFIG_LIBPULSE=0)
        add_compile_definitions(CONFIG_LIBRABBITMQ=0)
        add_compile_definitions(CONFIG_LIBRAV1E=0)
        add_compile_definitions(CONFIG_LIBRIST=0)
        add_compile_definitions(CONFIG_LIBRSVG=0)
        add_compile_definitions(CONFIG_LIBRTMP=0)
        add_compile_definitions(CONFIG_LIBSHADERC=0)
        add_compile_definitions(CONFIG_LIBSHINE=0)
        add_compile_definitions(CONFIG_LIBSMBCLIENT=0)
        add_compile_definitions(CONFIG_LIBSNAPPY=0)
        add_compile_definitions(CONFIG_LIBSOXR=0)
        add_compile_definitions(CONFIG_LIBSPEEX=0)
        add_compile_definitions(CONFIG_LIBSRT=0)
        add_compile_definitions(CONFIG_LIBSSH=0)
        add_compile_definitions(CONFIG_LIBSVTAV1=0)
        add_compile_definitions(CONFIG_LIBTENSORFLOW=0)
        add_compile_definitions(CONFIG_LIBTESSERACT=0)
        add_compile_definitions(CONFIG_LIBTHEORA=0)
        add_compile_definitions(CONFIG_LIBTWOLAME=0)
        add_compile_definitions(CONFIG_LIBUAVS3D=0)
        add_compile_definitions(CONFIG_LIBV4L2=0)
        add_compile_definitions(CONFIG_LIBVMAF=0)
        add_compile_definitions(CONFIG_LIBVORBIS=1)
        add_compile_definitions(CONFIG_LIBVPX=1)
        add_compile_definitions(CONFIG_LIBWEBP=0)
        add_compile_definitions(CONFIG_LIBXML2=0)
        add_compile_definitions(CONFIG_LIBZIMG=0)
        add_compile_definitions(CONFIG_LIBZMQ=0)
        add_compile_definitions(CONFIG_LIBZVBI=0)
        add_compile_definitions(CONFIG_LV2=0)
        add_compile_definitions(CONFIG_MEDIACODEC=0)
        add_compile_definitions(CONFIG_OPENAL=0)
        add_compile_definitions(CONFIG_OPENGL=0)
        add_compile_definitions(CONFIG_OPENSSL=0)
        add_compile_definitions(CONFIG_POCKETSPHINX=0)
        add_compile_definitions(CONFIG_VAPOURSYNTH=0)
        add_compile_definitions(CONFIG_ALSA=0)
        add_compile_definitions(CONFIG_APPKIT=0)
        add_compile_definitions(CONFIG_AVFOUNDATION=0)
        add_compile_definitions(CONFIG_BZLIB=0)
        add_compile_definitions(CONFIG_COREIMAGE=0)
        add_compile_definitions(CONFIG_ICONV=1)
        add_compile_definitions(CONFIG_LIBXCB=1)
        add_compile_definitions(CONFIG_LIBXCB_SHM=1)
        add_compile_definitions(CONFIG_LIBXCB_SHAPE=1)
        add_compile_definitions(CONFIG_LIBXCB_XFIXES=1)
        add_compile_definitions(CONFIG_LZMA=0)
        add_compile_definitions(CONFIG_MEDIAFOUNDATION=0)
        add_compile_definitions(CONFIG_METAL=0)
        add_compile_definitions(CONFIG_SCHANNEL=0)
        add_compile_definitions(CONFIG_SDL2=1)
        add_compile_definitions(CONFIG_SECURETRANSPORT=0)
        add_compile_definitions(CONFIG_SNDIO=1)
        add_compile_definitions(CONFIG_XLIB=1)
        add_compile_definitions(CONFIG_ZLIB=1)
        add_compile_definitions(CONFIG_CUDA_NVCC=0)
        add_compile_definitions(CONFIG_CUDA_SDK=0)
        add_compile_definitions(CONFIG_LIBNPP=0)
        add_compile_definitions(CONFIG_LIBMFX=0)
        add_compile_definitions(CONFIG_LIBVPL=0)
        add_compile_definitions(CONFIG_MMAL=0)
        add_compile_definitions(CONFIG_OMX=0)
        add_compile_definitions(CONFIG_OPENCL=0)
        add_compile_definitions(CONFIG_AMF=0)
        add_compile_definitions(CONFIG_AUDIOTOOLBOX=0)
        add_compile_definitions(CONFIG_CRYSTALHD=0)
        add_compile_definitions(CONFIG_CUDA=0)
        add_compile_definitions(CONFIG_CUDA_LLVM=0)
        add_compile_definitions(CONFIG_CUVID=0)
        add_compile_definitions(CONFIG_D3D11VA=0)
        add_compile_definitions(CONFIG_DXVA2=0)
        add_compile_definitions(CONFIG_FFNVCODEC=0)
        add_compile_definitions(CONFIG_NVDEC=0)
        add_compile_definitions(CONFIG_NVENC=0)
        add_compile_definitions(CONFIG_VAAPI=0)
        add_compile_definitions(CONFIG_VDPAU=0)
        add_compile_definitions(CONFIG_VIDEOTOOLBOX=0)
        add_compile_definitions(CONFIG_VULKAN=0)
        add_compile_definitions(CONFIG_V4L2_M2M=1)
        add_compile_definitions(CONFIG_FTRAPV=0)
        add_compile_definitions(CONFIG_GRAY=0)
        add_compile_definitions(CONFIG_HARDCODED_TABLES=0)
        add_compile_definitions(CONFIG_OMX_RPI=0)
        add_compile_definitions(CONFIG_RUNTIME_CPUDETECT=1)
        add_compile_definitions(CONFIG_SAFE_BITSTREAM_READER=1)
        add_compile_definitions(CONFIG_SHARED=0)
        add_compile_definitions(CONFIG_SMALL=0)
        add_compile_definitions(CONFIG_STATIC=1)
        add_compile_definitions(CONFIG_SWSCALE_ALPHA=1)
        add_compile_definitions(CONFIG_GPL=1)
        add_compile_definitions(CONFIG_NONFREE=0)
        add_compile_definitions(CONFIG_VERSION3=0)
        add_compile_definitions(CONFIG_AVDEVICE=1)
        add_compile_definitions(CONFIG_AVFILTER=1)
        add_compile_definitions(CONFIG_SWSCALE=1)
        add_compile_definitions(CONFIG_POSTPROC=1)
        add_compile_definitions(CONFIG_AVFORMAT=1)
        add_compile_definitions(CONFIG_AVCODEC=1)
        add_compile_definitions(CONFIG_SWRESAMPLE=1)
        add_compile_definitions(CONFIG_AVUTIL=1)
        add_compile_definitions(CONFIG_FFPLAY=0)
        add_compile_definitions(CONFIG_FFPROBE=0)
        add_compile_definitions(CONFIG_FFMPEG=0)
        add_compile_definitions(CONFIG_DCT=1)
        add_compile_definitions(CONFIG_DWT=1)
        add_compile_definitions(CONFIG_ERROR_RESILIENCE=1)
        add_compile_definitions(CONFIG_FAAN=1)
        add_compile_definitions(CONFIG_FAST_UNALIGNED=1)
        add_compile_definitions(CONFIG_FFT=1)
        add_compile_definitions(CONFIG_LSP=1)
        add_compile_definitions(CONFIG_MDCT=0)
        add_compile_definitions(CONFIG_PIXELUTILS=1)
        add_compile_definitions(CONFIG_NETWORK=1)
        add_compile_definitions(CONFIG_RDFT=1)
        add_compile_definitions(CONFIG_AUTODETECT=0)
        add_compile_definitions(CONFIG_FONTCONFIG=0)
        add_compile_definitions(CONFIG_LARGE_TESTS=1)
        add_compile_definitions(CONFIG_LINUX_PERF=0)
        add_compile_definitions(CONFIG_MACOS_KPERF=0)
        add_compile_definitions(CONFIG_MEMORY_POISONING=0)
        add_compile_definitions(CONFIG_NEON_CLOBBER_TEST=0)
        add_compile_definitions(CONFIG_OSSFUZZ=0)
        add_compile_definitions(CONFIG_PIC=1)
        add_compile_definitions(CONFIG_PTX_COMPRESSION=1)
        add_compile_definitions(CONFIG_THUMB=0)
        add_compile_definitions(CONFIG_VALGRIND_BACKTRACE=0)
        add_compile_definitions(CONFIG_XMM_CLOBBER_TEST=0)
        add_compile_definitions(CONFIG_BSFS=1)
        add_compile_definitions(CONFIG_DECODERS=1)
        add_compile_definitions(CONFIG_ENCODERS=1)
        add_compile_definitions(CONFIG_HWACCELS=0) # changed from 1
        add_compile_definitions(CONFIG_PARSERS=1)
        add_compile_definitions(CONFIG_INDEVS=0)
        add_compile_definitions(CONFIG_OUTDEVS=0)
        add_compile_definitions(CONFIG_FILTERS=1)
        add_compile_definitions(CONFIG_DEMUXERS=1)
        add_compile_definitions(CONFIG_MUXERS=1)
        add_compile_definitions(CONFIG_PROTOCOLS=1)
        add_compile_definitions(CONFIG_AANDCTTABLES=1)
        add_compile_definitions(CONFIG_AC3DSP=1)
        add_compile_definitions(CONFIG_ADTS_HEADER=1)
        add_compile_definitions(CONFIG_ATSC_A53=1)
        add_compile_definitions(CONFIG_AUDIO_FRAME_QUEUE=1)
        add_compile_definitions(CONFIG_AUDIODSP=1)
        add_compile_definitions(CONFIG_BLOCKDSP=1)
        add_compile_definitions(CONFIG_BSWAPDSP=1)
        add_compile_definitions(CONFIG_CABAC=1)
        add_compile_definitions(CONFIG_CBS=1)
        add_compile_definitions(CONFIG_CBS_AV1=1)
        add_compile_definitions(CONFIG_CBS_H264=1)
        add_compile_definitions(CONFIG_CBS_H265=1)
        add_compile_definitions(CONFIG_CBS_JPEG=1)
        add_compile_definitions(CONFIG_CBS_MPEG2=1)
        add_compile_definitions(CONFIG_CBS_VP9=1)
        add_compile_definitions(CONFIG_DEFLATE_WRAPPER=1)
        add_compile_definitions(CONFIG_DIRAC_PARSE=1)
        add_compile_definitions(CONFIG_DNN=1)
        add_compile_definitions(CONFIG_DOVI_RPU=1)
        add_compile_definitions(CONFIG_DVPROFILE=1)
        add_compile_definitions(CONFIG_EXIF=1)
        add_compile_definitions(CONFIG_FAANDCT=1)
        add_compile_definitions(CONFIG_FAANIDCT=1)
        add_compile_definitions(CONFIG_FDCTDSP=1)
        add_compile_definitions(CONFIG_FMTCONVERT=1)
        add_compile_definitions(CONFIG_FRAME_THREAD_ENCODER=1)
        add_compile_definitions(CONFIG_G722DSP=1)
        add_compile_definitions(CONFIG_GOLOMB=1)
        add_compile_definitions(CONFIG_GPLV3=0)
        add_compile_definitions(CONFIG_H263DSP=1)
        add_compile_definitions(CONFIG_H264CHROMA=1)
        add_compile_definitions(CONFIG_H264DSP=1)
        add_compile_definitions(CONFIG_H264PARSE=1)
        add_compile_definitions(CONFIG_H264PRED=1)
        add_compile_definitions(CONFIG_H264QPEL=1)
        add_compile_definitions(CONFIG_H264_SEI=1)
        add_compile_definitions(CONFIG_HEVCPARSE=1)
        add_compile_definitions(CONFIG_HEVC_SEI=1)
        add_compile_definitions(CONFIG_HPELDSP=1)
        add_compile_definitions(CONFIG_HUFFMAN=1)
        add_compile_definitions(CONFIG_HUFFYUVDSP=1)
        add_compile_definitions(CONFIG_HUFFYUVENCDSP=1)
        add_compile_definitions(CONFIG_IDCTDSP=1)
        add_compile_definitions(CONFIG_IIRFILTER=1)
        add_compile_definitions(CONFIG_INFLATE_WRAPPER=1)
        add_compile_definitions(CONFIG_INTRAX8=1)
        add_compile_definitions(CONFIG_ISO_MEDIA=1)
        add_compile_definitions(CONFIG_IVIDSP=1)
        add_compile_definitions(CONFIG_JPEGTABLES=1)
        add_compile_definitions(CONFIG_LGPLV3=0)
        add_compile_definitions(CONFIG_LIBX262=0)
        add_compile_definitions(CONFIG_LLAUDDSP=1)
        add_compile_definitions(CONFIG_LLVIDDSP=1)
        add_compile_definitions(CONFIG_LLVIDENCDSP=1)
        add_compile_definitions(CONFIG_LPC=1)
        add_compile_definitions(CONFIG_LZF=1)
        add_compile_definitions(CONFIG_ME_CMP=1)
        add_compile_definitions(CONFIG_MPEG_ER=1)
        add_compile_definitions(CONFIG_MPEGAUDIO=1)
        add_compile_definitions(CONFIG_MPEGAUDIODSP=1)
        add_compile_definitions(CONFIG_MPEGAUDIOHEADER=1)
        add_compile_definitions(CONFIG_MPEG4AUDIO=1)
        add_compile_definitions(CONFIG_MPEGVIDEO=1)
        add_compile_definitions(CONFIG_MPEGVIDEODEC=1)
        add_compile_definitions(CONFIG_MPEGVIDEOENC=1)
        add_compile_definitions(CONFIG_MSMPEG4DEC=1)
        add_compile_definitions(CONFIG_MSMPEG4ENC=1)
        add_compile_definitions(CONFIG_MSS34DSP=1)
        add_compile_definitions(CONFIG_PIXBLOCKDSP=1)
        add_compile_definitions(CONFIG_QPELDSP=1)
        add_compile_definitions(CONFIG_QSV=0)
        add_compile_definitions(CONFIG_QSVDEC=0)
        add_compile_definitions(CONFIG_QSVENC=0)
        add_compile_definitions(CONFIG_QSVVPP=0)
        add_compile_definitions(CONFIG_RANGECODER=1)
        add_compile_definitions(CONFIG_RIFFDEC=1)
        add_compile_definitions(CONFIG_RIFFENC=1)
        add_compile_definitions(CONFIG_RTPDEC=1)
        add_compile_definitions(CONFIG_RTPENC_CHAIN=1)
        add_compile_definitions(CONFIG_RV34DSP=1)
        add_compile_definitions(CONFIG_SCENE_SAD=1)
        add_compile_definitions(CONFIG_SINEWIN=1)
        add_compile_definitions(CONFIG_SNAPPY=1)
        add_compile_definitions(CONFIG_SRTP=1)
        add_compile_definitions(CONFIG_STARTCODE=1)
        add_compile_definitions(CONFIG_TEXTUREDSP=1)
        add_compile_definitions(CONFIG_TEXTUREDSPENC=1)
        add_compile_definitions(CONFIG_TPELDSP=1)
        add_compile_definitions(CONFIG_VAAPI_1=1)
        add_compile_definitions(CONFIG_VAAPI_ENCODE=1)
        add_compile_definitions(CONFIG_VC1DSP=1)
        add_compile_definitions(CONFIG_VIDEODSP=1)
        add_compile_definitions(CONFIG_VP3DSP=1)
        add_compile_definitions(CONFIG_VP56DSP=1)
        add_compile_definitions(CONFIG_VP8DSP=1)
        add_compile_definitions(CONFIG_WMA_FREQS=1)
        add_compile_definitions(CONFIG_WMV2DSP=1)
    endif() # use cmake defines instead of NASM -P
    set(CMAKE_EXECUTE_PROCESS_COMMAND_ECHO STDOUT)
    
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -z noexecstack")
    
    # module path is needed for ConfigStaticDependLib.cmake
    list(APPEND CMAKE_MODULE_PATH "${PROJECT_SRC_ROOT_DIR}/cmake/cmake_helpers")  
    
    set(NLC_OS_TYPE linux)
    # set(NLC_BUILD_TYPE debug)
    set(NLC_BUILD_TYPE release)
    set(NLC_CPU_TYPE x64)
    set(NLC_CPU "CPU_X64")
    set(NLC_CPU_BITS "64")
    
    enable_language(ASM_NASM)
    set(CMAKE_ASM_NASM_OBJECT_FORMAT elf64)
    set(CMAKE_ASM_NASM_SOURCE_FILE_EXTENSIONS nasm asm)
    
    set(CMAKE_ASM_NASM_COMPILE_OBJECT "<CMAKE_ASM_NASM_COMPILER> <DEFINES> <INCLUDES> -f ${CMAKE_ASM_NASM_OBJECT_FORMAT} -o <OBJECT> <SOURCE>")
    set(CMAKE_DEPFILE_FLAGS_ASM_NASM "-MD <DEP_FILE> -MT <DEP_TARGET>")
    string(APPEND CMAKE_ASM_NASM_FLAGS " -DPIC -F dwarf --DARCH_X86_64=1 ")   
        
    message(STATUS "Adding subdir nolimitlib")
    add_subdirectory(nolimitlib)

    message(STATUS "Adding subdir nolimitgui")
    add_subdirectory(nolimitgui)
    
    message(STATUS "command line make Flatpak nolimitconnect config done")
else()
    if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
        set(USER_HOME_DIR $ENV{USERPROFILE})
        message(STATUS "User Windows Home Directory: ${USER_HOME_DIR}")
    elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
        set(USER_HOME_DIR $ENV{HOME})
        message(STATUS "User Linux Home Directory: ${USER_HOME_DIR}")
    else()
        message(FATAL_ERROR "Build host is Unknown ${CMAKE_HOST_SYSTEM_NAME}")
    endif()
        message(STATUS "1 - CMAKE_PREFIX_PATH ${CMAKE_PREFIX_PATH}")

    #defaults
    set(NLC_QT "6.4.3")
    set(NLC_POSTPATH "/gcc_64")

    set(NLC_CPU "CPU_X64")
    set(NLC_CPU_BITS "64")

    if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
        message(STATUS "Build host OS Windows")
        set(NLC_PREPATH "F:/Qt/")
        set(TARGET_OS_ANDROID true)    
        if(DEFINED ANDROID_NDK)
            message(STATUS "Command line NDK " ${ANDROID_NDK})
        else()
            set(CMAKE_ANDROID_NDK F:/Android/android-sdk/ndk/25.1.8937393)
        endif()

    elseif(DEFINED MAKE_ENV)
        message(STATUS "Build host GitLab")
        set(NLC_PREPATH "/opt/qt/")

    elseif(DEFINED QT_VERSION)
        message(STATUS "Build host Command Line Linux")
        message(STATUS "Build host OS Linux")
        set(NLC_PREPATH "/home/nolimit/Qt/")
        if(${QT_VERSION} STREQUAL "7_0_2")
            set(NLC_QT_VER "7.0.2")
        elseif(${QT_VERSION} STREQUAL "7_0_1")
            set(NLC_QT_VER "7.0.1")
        elseif(${QT_VERSION} STREQUAL "6_6_4")
            set(NLC_QT_VER "6.6.4")
        elseif(${QT_VERSION} STREQUAL "6_6_3")
            set(NLC_QT_VER "6.6.3")
        elseif(${QT_VERSION} STREQUAL "6_6_2")
            set(NLC_QT_VER "6.6.2")
        elseif(${QT_VERSION} STREQUAL "6_6_1")
            set(NLC_QT_VER "6.6.1") 
        else()
            message(STATUS "Unknown command line QT_VERSION " ${QT_VERSION})
        endif()

    else()
        message(STATUS "Build host OS Linux")
        set(NLC_PREPATH "/home/nolimit/Qt/")

    endif()

    # when run from command line
    if(DEFINED ARM32)
        set(NLC_CPU "CPU_ARM32")
        set(NLC_CPU_BITS "32")
        set(NLC_POSTPATH "/android_armv7")
    elseif(DEFINED ARM64)
        set(NLC_CPU "CPU_ARM64")
        set(NLC_POSTPATH "/android_arm64_v8a")
    else()
        set(NLC_CPU "CPU_X64")
    endif()

    # when run from within QtCreator
    if(DEFINED ANDROID_ABI)
        if(ANDROID_ABI STREQUAL "armeabi-v7a")
            set(NLC_CPU "CPU_ARM32")
            set(NLC_CPU_BITS "32")
        elseif(ANDROID_ABI STREQUAL "arm64-v8a")
            set(NLC_CPU "CPU_ARM64")
            set(NLC_CPU_BITS "64")
        else()
            message(STATUS "Unknown Android ABI ")
        endif()

        set(POS1 -1)
        if(DEFINED CMAKE_PREFIX_PATH)
            STRING( FIND "${CMAKE_PREFIX_PATH}" "6.6.2" POS1)
            if( POS1 GREATER -1)
                message(STATUS "PREFIX_PATH contains 6.6.2 ")
                set(NLC_QT_VER " 6.6.2")
            else()  
                set(POS2 -1)
                STRING( FIND "${CMAKE_PREFIX_PATH}" "6.6.3" POS2)
                if( POS2 GREATER -1)
                    message(STATUS "PREFIX_PATH contains 6.6.3 ")
                    set(NLC_QT_VER " 6.6.3")

                else()
                    set(POS2 -1)
                    STRING( FIND "${CMAKE_PREFIX_PATH}" "6.6.4" POS2)
                    if( POS2 GREATER -1)
                        message(STATUS "PREFIX_PATH contains 6.6.4 ")
                        set(NLC_QT_VER " 6.6.4")

                    else()
                        set(POS2 -1)
                        STRING( FIND "${CMAKE_PREFIX_PATH}" "7.0.0" POS2)
                        if( POS2 GREATER -1)
                            message(STATUS "PREFIX_PATH contains 7.0.0 ")
                            set(NLC_QT_VER " 7.0.0")

                        else()
                            set(POS3 -1)
                            STRING( FIND "${CMAKE_PREFIX_PATH}" "7.0.1" POS3)
                            if( POS3 GREATER -1)
                                message(STATUS "PREFIX_PATH contains 7.0.1 ")
                                set(NLC_QT_VER " 7.0.1")

                            else()
                                message(ERROR "PREFIX_PATH did not contain any known QT Version ")
                            endif()
                        endif()
                    endif()
                endif()
            endif()
        endif()

    else()
        set(ANDROID_ABI "x64")
    endif()

    message(STATUS "CPU TYPE " ${NLC_CPU})
    message(STATUS "ANDROID ABI " ${ANDROID_ABI})

    #=== detect build enviroment and target os ===#
    if(DEFINED MAKE_ENV)
        message(STATUS "*** MAKE_ENV (GitLab) ***")
        #=== assume called from gitlab ci pipeline.. linux only compile and cross compile of android
        set(CI_PIPELINE true)
        set(MANUAL_PACKAGE true)
        add_compile_definitions(CI_PIPELINE)
        message(STATUS "CI Pipeline Build")
        if(DEFINED TARGET_OS_ANDROID)
            set(TARGET_OS_ANDROID true)

            # turn off warnings or gitlab will stop logging
            set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -w")
            set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -w")
            add_compile_options (-w)

            if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
                message(STATUS "CI Build Target OS Android built on host OS Windows")
            elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
                message(STATUS "CI Build Target OS Android built on host OS Linux")
            else()
                message(FATAL_ERROR "CI Build host is Unknown OS attempting to compile Android")
            endif()
        elseif(DEFINED TARGET_OS_WINDOWS)
            set(TARGET_OS_WINDOWS true)

            message(STATUS "CI Build Target OS Windows built on host OS Windows")
        elseif(DEFINED TARGET_OS_LINUX)
            set(TARGET_OS_LINUX true)

            # turn off warnings or gitlab will stop logging
            add_compile_options(-Wno-everything)
            set(NLC_POSTPATH "/gcc_64")

            message(STATUS "CI Build Target OS Linux built on host OS Linux " ${CMAKE_PREFIX_PATH})

            enable_language(ASM_NASM)
            set(CMAKE_ASM_NASM_OBJECT_FORMAT elf64)
            set(CMAKE_ASM_NASM_SOURCE_FILE_EXTENSIONS nasm asm)

            set(CMAKE_ASM_NASM_COMPILE_OBJECT "<CMAKE_ASM_NASM_COMPILER> <FLAGS> <DEFINES> <INCLUDES> -f ${CMAKE_ASM_NASM_OBJECT_FORMAT} -o <OBJECT> <SOURCE>")
            set(CMAKE_DEPFILE_FLAGS_ASM_NASM "-MD <DEP_FILE> -MT <DEP_TARGET>")
            string(APPEND CMAKE_ASM_NASM_FLAGS " -DPIC -F dwarf -DARCH_X86_64=1 ")
        endif()

    elseif(DEFINED CMAKE_PREFIX_PATH)
        message(STATUS "*** CMAKE_PREFIX_PATH (QtCreator) *** ${CMAKE_PREFIX_PATH}")
        #=== assumes cmake was called from QCreator
        if(DEFINED ANDROID_NDK)
            set(TARGET_OS_ANDROID true)
            if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
                message(STATUS "QCreator Build Target OS Android built on host OS Windows")
            elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
                message(STATUS "QCreator Build Target OS Android built on host OS Linux")
            else()
                message(FATAL_ERROR "QCreator Build host is Unknown OS attempting to compile Android")
            endif()

        elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
            set(TARGET_OS_WINDOWS true)
            message(STATUS "QCreator Build Target OS Windows built on host OS Windows")

        elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
            set(TARGET_OS_LINUX true)
            message(STATUS "QCreator Build Target OS Linux built on host OS Linux")

            enable_language(ASM_NASM)
            set(CMAKE_ASM_NASM_OBJECT_FORMAT elf64)
            set(CMAKE_ASM_NASM_SOURCE_FILE_EXTENSIONS nasm asm)

            set(CMAKE_ASM_NASM_COMPILE_OBJECT "<CMAKE_ASM_NASM_COMPILER> <FLAGS> <DEFINES> <INCLUDES> -f ${CMAKE_ASM_NASM_OBJECT_FORMAT} -o <OBJECT> <SOURCE>")
            set(CMAKE_DEPFILE_FLAGS_ASM_NASM "-MD <DEP_FILE> -MT <DEP_TARGET>")
            string(APPEND CMAKE_ASM_NASM_FLAGS " -DPIC -F dwarf -DARCH_X86_64=1 ")
        endif()

    else()
        message(STATUS "*** CMD_LINE_MAKE ***")
        #=== assumes cmake was called from command line and needs the enviroment setup

        #set define so can deterimine if need additional enviroment params missing in command line compile
        set(CMD_LINE_MAKE true)
        set(MANUAL_PACKAGE true)
        add_compile_definitions(CMD_LINE_MAKE)

        set(CMAKE_GENERATOR "Unix Makefiles")

        if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
           message(STATUS "Build host OS Windows")
            if(TARGET_OS_LINUX)
                message(FATAL_ERROR "Target OS Linux built project(nolimitconnect LANGUAGES C CXX ASM)on Windows OS is NOT supported")
            elseif(TARGET_OS_ANDROID)
                set(CMAKE_ANDROID_NDK F:/Android/android-sdk/ndk/25.1.8937393)
                if(${NLC_CPU_BITS} STREQUAL "32")
                    set(ARM_ASM ${CMAKE_ANDROID_NDK}/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi24-clang)
                else()
                    set(ARM_ASM ${CMAKE_ANDROID_NDK}/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android24-clang)       
                endif()

                set(ARM_SYSROOT --sysroot=${CMAKE_ANDROID_NDK}/toolchains/llvm/prebuilt/linux-x86_64/sysroot)
           
            elseif(TARGET_OS_WINDOWS)
                set(NLC_POST_PATH, "msvc2019_64")

                list(APPEND CMAKE_PREFIX_PATH  ${NLC_PREPATH} ${NLC_QT_VER} ${NLC_POSTPATH})
                message(STATUS "qt prefix path ${CMAKE_PREFIX_PATH}")

                set(CMAKE_ASM_NASM_OBJECT_FORMAT win64)
                set(CMAKE_ASM_NASM_SOURCE_FILE_EXTENSIONS nasm asm)
                enable_language(ASM_NASM)
                set(CMAKE_ASM_NASM_COMPILE_OBJECT "<CMAKE_ASM_NASM_COMPILER> <DEFINES> <INCLUDES> -f ${CMAKE_ASM_NASM_OBJECT_FORMAT} -o <OBJECT> <SOURCE>")
                set(CMAKE_DEPFILE_FLAGS_ASM_NASM "-MD <DEP_FILE> -MT <DEP_TARGET>")

            else()
               message(FATAL_ERROR "TARGET OS is not defined.. caller must define TARGET_OS_LINUX, TARGET_OS_WINDOWS or TARGET_OS_ANDROID")
            endif()

        elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
            message(STATUS "Build host OS Linux")
            if(TARGET_OS_LINUX)

                message(STATUS "qt prefix path ${CMAKE_PREFIX_PATH}")

                enable_language(ASM_NASM)
                project(nolimitconnect LANGUAGES C CXX ASM)
                set(CMAKE_ASM_NASM_OBJECT_FORMAT elf64)
                set(CMAKE_ASM_NASM_SOURCE_FILE_EXTENSIONS nasm asm)

                set(CMAKE_ASM_NASM_COMPILE_OBJECT "<CMAKE_ASM_NASM_COMPILER> <FLAGS> <DEFINES> <INCLUDES> -f ${CMAKE_ASM_NASM_OBJECT_FORMAT} -o <OBJECT> <SOURCE>")
                set(CMAKE_DEPFILE_FLAGS_ASM_NASM "-MD <DEP_FILE> -MT <DEP_TARGET>")
                string(APPEND CMAKE_ASM_NASM_FLAGS " -DPIC -F dwarf -DARCH_X86_64=1 ")

           elseif(TARGET_OS_ANDROID)
     
               set(CMAKE_ANDROID_NDK /home/nolimit/Android/Sdk/ndk/25.1.8937393)
               
               set(ANDROID_SDK_ROOT /home/nolimit/Android/Sdk)

               set(ARM_SYSROOT --sysroot=${CMAKE_ANDROID_NDK}/toolchains/llvm/prebuilt/linux-x86_64/sysroot)
                if(${NLC_CPU_BITS} STREQUAL "32")
                    set(ARM_ASM ${CMAKE_ANDROID_NDK}/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi24-clang)
                else()
                    set(ARM_ASM ${CMAKE_ANDROID_NDK}/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android24-clang)       
                endif()

           elseif(TARGET_OS_WINDOWS)
                message(FATAL_ERROR "Target OS Windows built on Linux OS is not yet supported")
           else()
               message(FATAL_ERROR "TARGET OS is not defined.. caller must define TARGET_OS_LINUX, TARGET_OS_WINDOWS or TARGET_OS_ANDROID")
           endif()
        else()
           message(FATAL_ERROR "Build host is Unknown OS")
        endif()
    endif()

    if(DEFINED TARGET_OS_WINDOWS)
        message(STATUS "*** TARGET_OS_WINDOWS ***")
        add_compile_definitions(TARGET_OS_WINDOWS)
        set(TARGET_CPU_X64 true)
        set(PROJECT_BIN_DIR ${PROJECT_SRC_ROOT_DIR}/nolimitgui/bin-Windows)
        set(PROJECT_ASSETS_DIR ${PROJECT_SRC_ROOT_DIR}/nolimitgui/bin-Windows/assets)

        if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
            message(STATUS "TARGET_OS_WINDOWS Build Target OS Windows built on host OS Windows")
        elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
            message(STATUS "TARGET_OS_WINDOWS Build Target OS Windows built on host OS Linux is not yet supported")
        else()
            message(FATAL_ERROR "Build host is Unknown OS attempting to compile Windows")
        endif()

    elseif(DEFINED TARGET_OS_LINUX)
        message(STATUS "*** TARGET_OS_LINUX ***")
        add_compile_definitions(TARGET_OS_LINUX TARGET_CPU_X86_64 TARGET_POSIX PIC)
        set(TARGET_CPU_X86_64 true) #so can use in cmake as if(DEFINED TARGET_CPU_X86_64) statement
        set(TARGET_POSIX true)

        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fpermissive")

        set(PROJECT_BIN_DIR ${PROJECT_SRC_ROOT_DIR}/nolimitgui/bin-Linux)
        set(PROJECT_ASSETS_DIR ${PROJECT_SRC_ROOT_DIR}/nolimitgui/bin-Linux/assets)

        # create the directories the qt shared libraries will be copied to for debian packaging
        file(MAKE_DIRECTORY "${PROJECT_BIN_DIR}/usr")
        file(MAKE_DIRECTORY "${PROJECT_BIN_DIR}/usr/lib")
        if(WIN32)
            message(STATUS "TARGET_OS_WINDOWS Build Target OS Linux built on host OS Windows is not supported")
        elseif(UNIX)
            message(STATUS "TARGET_OS_LINUX Build Target OS Linux built on host OS Linux")
        else()
            message(FATAL_ERROR "Build host is Unknown OS attempting to compile Linux")
        endif()

    elseif(DEFINED TARGET_OS_ANDROID)
        message(STATUS "*** TARGET_OS_ANDROID ***")
        add_compile_definitions(ANDROID)
        set(CMAKE_SYSTEM_NAME Android)
        set(ANDROID_PLATFORM "android-24")
        #set(CMAKE_SYSTEM_VERSION 33) # API level

        set(ANDROID_STL c++_static)

        set(TARGET_POSIX true)
        set(PROJECT_BIN_DIR ${PROJECT_SRC_ROOT_DIR}/bin-Android)
        set(PROJECT_ASSETS_DIR ${PROJECT_SRC_ROOT_DIR}/nolimitgui/bin-Android/assets)

        set(ANDROID_APK_OUTPUT_DIR ${PROJECT_PACKAGE_DIR})

        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fpermissive")

        if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
            message(STATUS "Build Target OS Android built on host OS Windows")
        elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
            message(STATUS "Build Target OS Android built on host OS Linux")
        else()
            message(FATAL_ERROR "Build host is Unknown OS attempting to compile Android")
        endif()

    else()
        message(FATAL_ERROR "TARGET OS is not defined.. caller must define TARGET_OS_LINUX, TARGET_OS_WINDOWS or TARGET_OS_ANDROID")
    endif()

    if(CMD_LINE_MAKE)
        message(STATUS "*** CMD_LINE_MAKE ***")

        if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
            message(STATUS "HOST OS Windows")

            if(DEFINED TARGET_OS_ANDROID)
                # path should be set correctly already

                message(STATUS "CMD_LINE_MAKE Target Android on os windows")

            elseif(DEFINED TARGET_OS_WINDOWS)
                if(${CMAKE_CXX_COMPILER_ID} STREQUAL "MSVC")
                    set(NLC_POSTPATH "/msvc2019_64")
                    
                    message(STATUS "CMD_LINE_MAKE Compiling windows with MSVC")
                else()
                    set(NLC_POSTPATH "/mingw_64")
                    message(STATUS "CMD_LINE_MAKE Compiling windows with mingw")
                endif()

                list(APPEND CMAKE_PREFIX_PATH "${NLC_PREPATH}${NLC_QT_VER}${NLC_POSTPATH}")

            else()
                message("CMD_LINE_MAKE TARGET UNKNOWN")
            endif()

        elseif(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
            message("HOST OS Linux")
            if(DEFINED TARGET_OS_ANDROID)
                # path should be set correctly already
                list(APPEND CMAKE_PREFIX_PATH "${NLC_PREPATH}${NLC_QT_VER}${NLC_POSTPATH}")
                
                message(STATUS "CMD_LINE_MAKE Target Android")

            elseif(DEFINED TARGET_OS_LINUX)
                # path should be set correctly already
                message(STATUS "*** BEFORE CMD_LINE_MAKE Target Linux with prefix ${CMAKE_PREFIX_PATH}")
                list(APPEND CMAKE_PREFIX_PATH "${NLC_PREPATH}${NLC_QT_VER}${NLC_POSTPATH}")
                
                message(STATUS "*** AFTER CMD_LINE_MAKE Target Linux with prefix ${CMAKE_PREFIX_PATH}")

            else()
                message("CMD_LINE_MAKE TARGET UNKNOWN")
            endif()

        else()
            message("CMD_LINE_MAKE CMAKE_HOST_SYSTEM_NAME UNKNOWN")
        endif()
    endif()

    if(CI_PIPELINE)   
        message(STATUS "*** CI_PIPELINE ***")
        if(QT_VERSION STREQUAL 6_6_4)
           if(DEFINED TARGET_OS_ANDROID)
               set(CMAKE_ANDROID_NDK "/opt/android-sdk/ndk/25.1.8937393")
               if(DEFINED ARM32)
                   set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.4/android_armv7")
               else()
                   set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.4/android_arm64_v8a")
               endif()

               list(APPEND CMAKE_FIND_ROOT_PATH "${CMAKE_PREFIX_PATH}")
               set(ANDROID_SDK_ROOT "/opt/android-sdk")

               set(CMAKE_TOOLCHAIN_FILE "${CMAKE_ANDROID_NDK}/build/cmake/android.toolchain.cmake")
               message(STATUS "CI Pipeline Target Android")

           elseif(DEFINED TARGET_OS_LINUX)
               set(CMAKE_PREFIX_PATH "/opt/Qt-amd64-6.6.4")
               message(STATUS "CI Pipeline Target Linux")
           endif()
        elseif(QT_VERSION STREQUAL 6_6_3)
           if(DEFINED TARGET_OS_ANDROID)
               set(CMAKE_ANDROID_NDK "/opt/android-sdk/ndk/25.1.8937393")
               if(DEFINED ARM32)
                   set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.3/android_armv7")
               else()
                   set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.3/android_arm64_v8a")
               endif()

               list(APPEND CMAKE_FIND_ROOT_PATH "${CMAKE_PREFIX_PATH}")
               set(ANDROID_SDK_ROOT "/opt/android-sdk")

               set(CMAKE_TOOLCHAIN_FILE "${CMAKE_ANDROID_NDK}/build/cmake/android.toolchain.cmake")
               message(STATUS "CI Pipeline Target Android")

           elseif(DEFINED TARGET_OS_LINUX)
               set(CMAKE_PREFIX_PATH "/opt/Qt-amd64-6.6.3")
               message(STATUS "CI Pipeline Target Linux")
           endif()
        elseif(QT_VERSION STREQUAL 6_6_2)
            if(DEFINED TARGET_OS_ANDROID)
                set(CMAKE_ANDROID_NDK "/opt/android-sdk/ndk/25.1.8937393")
                if(DEFINED ARM32)
                    set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.2/android_armv7")
                else()
                    set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.2/android_arm64_v8a")
                endif()
                
                list(APPEND CMAKE_FIND_ROOT_PATH "${CMAKE_PREFIX_PATH}")
                set(ANDROID_SDK_ROOT "/opt/android-sdk")
                
                set(CMAKE_TOOLCHAIN_FILE "${CMAKE_ANDROID_NDK}/build/cmake/android.toolchain.cmake")
                message(STATUS "CI Pipeline Target Android")


            elseif(DEFINED TARGET_OS_LINUX)
                set(CMAKE_PREFIX_PATH "/opt/Qt-amd64-6.6.2")
                message(STATUS "CI Pipeline Target Linux")
            endif()
            
        elseif(QT_VERSION STREQUAL 6_6_1)
            if(DEFINED TARGET_OS_ANDROID)
                set(CMAKE_ANDROID_NDK "/opt/android-sdk/ndk/25.1.8937393")
                if(DEFINED ARM32)
                    set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.1/android_armv7")
                else()
                    set(CMAKE_PREFIX_PATH "/opt/Qt-android-6.6.1/android_arm64_v8a")
                endif()
                
                list(APPEND CMAKE_FIND_ROOT_PATH "${CMAKE_PREFIX_PATH}")
                set(ANDROID_SDK_ROOT "/opt/android-sdk")
                
                set(CMAKE_TOOLCHAIN_FILE "${CMAKE_ANDROID_NDK}/build/cmake/android.toolchain.cmake")
                message(STATUS "CI Pipeline Target Android")


            elseif(DEFINED TARGET_OS_LINUX)
                set(CMAKE_PREFIX_PATH "/opt/Qt-amd64-6.6.1")
                message(STATUS "CI Pipeline Target Linux")
            endif()
            
        else()
            message("CI_PIPELINE TARGET UNKNOWN")
        endif()
    endif()

    if(DEFINED CMD_LINE_MAKE)
        #fix issue where depend lib player-nlc uses qt but from command line android cannot find the qt packages etc
        if(DEFINED TARGET_OS_ANDROID)
            if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
                list(APPEND CMAKE_FIND_ROOT_PATH "${CMAKE_PREFIX_PATH}")
                set(ANDROID_SDK_ROOT /home/nolimit/Android/Sdk)
                set(CMAKE_TOOLCHAIN_FILE "${CMAKE_ANDROID_NDK}/build/cmake/android.toolchain.cmake")
            endif()
        endif()
    endif()

    #=== project folders setup ===#
    if(DEFINED TARGET_OS_ANDROID)
        set(NLC_OS_TYPE android)
    elseif(DEFINED TARGET_OS_LINUX)
        set(NLC_OS_TYPE linux)
    elseif(DEFINED TARGET_OS_WINDOWS)
        set(NLC_OS_TYPE linux)
    endif()

    if(CMAKE_BUILD_TYPE MATCHES Debug)
        set(NLC_BUILD_TYPE debug)
    else()
        set(NLC_BUILD_TYPE release)
    endif()

    if(${NLC_CPU} STREQUAL "CPU_ARM32")
        set(NLC_CPU_TYPE arm32)
    elseif(${NLC_CPU} STREQUAL "CPU_ARM64")
        set(NLC_CPU_TYPE arm64)
    elseif(${NLC_CPU_BITS} STREQUAL "32")
        set(NLC_CPU_TYPE x86)
    else()
        set(NLC_CPU_TYPE x64)
    endif()

    # set project static libs directory
    file(MAKE_DIRECTORY "${PROJECT_SRC_ROOT_DIR}/build-staticlibs/${NLC_OS_TYPE}")
    file(MAKE_DIRECTORY "${PROJECT_SRC_ROOT_DIR}/build-staticlibs/${NLC_OS_TYPE}/${NLC_BUILD_TYPE}")
    file(MAKE_DIRECTORY "${PROJECT_SRC_ROOT_DIR}/build-staticlibs/${NLC_OS_TYPE}/${NLC_BUILD_TYPE}/${NLC_CPU_TYPE}")
    set(PROJECT_STATIC_LIB_DIR "${PROJECT_SRC_ROOT_DIR}/build-staticlibs/${NLC_OS_TYPE}/${NLC_BUILD_TYPE}/${NLC_CPU_TYPE}")
    set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${PROJECT_STATIC_LIB_DIR}")

    # set project package directory
    file(MAKE_DIRECTORY "${PROJECT_PACKAGE_ROOT_DIR}/${NLC_OS_TYPE}")
    file(MAKE_DIRECTORY "${PROJECT_PACKAGE_ROOT_DIR}/${NLC_OS_TYPE}/${NLC_BUILD_TYPE}")
    file(MAKE_DIRECTORY "${PROJECT_PACKAGE_ROOT_DIR}/${NLC_OS_TYPE}/${NLC_BUILD_TYPE}/${NLC_CPU_TYPE}")
    set(PROJECT_PACKAGE_DIR "${PROJECT_PACKAGE_ROOT_DIR}/${NLC_OS_TYPE}/${NLC_BUILD_TYPE}/${NLC_CPU_TYPE}")


    # defines for all projects
    if(${NLC_CPU} STREQUAL "CPU_ARM32")
        #add_compile_options(-Wno-dev) #suppress dev warnings which even Qt spits out when compiling for armeabi-v7a

        set(NLC_POSTPATH "/android_armv7")
        set(ANDROID_ABI "armeabi-v7a")
        set(CMAKE_ANDROID_ARCH_ABI armeabi-v7a)
        message(STATUS "CMAKE_ANDROID_ARCH_ABI " ${CMAKE_ANDROID_ARCH_ABI})

        set(CMAKE_ASM_FLAGS "${CMAKE_ASM_FLAGS} -march=armv7-a -mtune=cortex-a9 --target=arm-linux-android23 -D_ISOC99_SOURCE -D_FILE_OFFSET_BITS=64 -D_LARGEFILE_SOURCE -Dstrtod=avpriv_strtod -DPIC -DZLIB_CONST -O3 -fPIC -Qunused-arguments -MMD -MF" )
        set(TARGET_CPU_ARM true) # general cpu type
        add_compile_definitions(TARGET_OS_ANDROID TARGET_POSIX TARGET_CPU_ARM TARGET_CPU_32BIT)

    elseif(${NLC_CPU} STREQUAL "CPU_ARM64")
        set(NLC_POSTPATH "/android_arm64_v8a")
        set(ANDROID_ABI "arm64-v8a")
        set(CMAKE_ANDROID_ARCH_ABI arm64-v8a)
        message(STATUS "CMAKE_ANDROID_ARCH_ABI " ${CMAKE_ANDROID_ARCH_ABI})

        set(CMAKE_ASM_FLAGS "${CMAKE_ASM_FLAGS} --target=aarch64-linux-android23 -D_ISOC99_SOURCE -D_FILE_OFFSET_BITS=64 -D_LARGEFILE_SOURCE -Dstrtod=avpriv_strtod -DPIC -DZLIB_CONST -O3 -fPIC -Qunused-arguments -MMD -MF" )
        set(TARGET_CPU_ARM true) # general cpu type
        set(TARGET_CPU_AARCH64 true)
        add_compile_definitions(TARGET_OS_ANDROID TARGET_POSIX TARGET_CPU_ARM TARGET_CPU_AARCH64 TARGET_CPU_64BIT)

    else()
        add_compile_definitions(TARGET_CPU_64BIT)
        add_compile_definitions(TARGET_CPU_X86_64)
    endif()

    if(DEFINED TARGET_OS_ANDROID)
        if(MANUAL_PACKAGE)
            message(STATUS "MANUAL_PACKAGE is defined ")
            set(ANDROID_STL c++_static)
            add_compile_definitions(ANDROID_STL=c++_static)
        endif()

        set(CMAKE_ANDROID_ASSETS_DIRECTORIES "${PROJECT_BIN_DIR}/assets")
        message(STATUS "CMAKE_ANDROID_ASSETS_DIRECTORIES " ${CMAKE_ANDROID_ASSETS_DIRECTORIES})

        project(nolimitconnect LANGUAGES C CXX ASM)

    else()
        project(nolimitconnect LANGUAGES C CXX)
    endif()

    #set(CMAKE_LIBRARY_OUTPUT_DIRECTORY "${PROJECT_BIN_DIR}") not relevant because no dll or shared libraries
    if(DEFINED TARGET_OS_LINUX)
        set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${PROJECT_BIN_DIR}")
    endif()

    set(Qt6_DIR "${CMAKE_PREFIX_PATH}/lib/cmake/Qt6")  

    # module path is needed for ConfigStaticDependLib.cmake
    list(APPEND CMAKE_MODULE_PATH "${PROJECT_SRC_ROOT_DIR}/cmake/cmake_helpers")  

    message(STATUS "CMAKE_PREFIX_PATH ${CMAKE_PREFIX_PATH}")
    message(STATUS "CMAKE_MODULE_PATH  ${CMAKE_MODULE_PATH}")
    message(STATUS "CMAKE_FIND_ROOT_PATH ${CMAKE_FIND_ROOT_PATH}")
    message(STATUS "CMAKE_TOOLCHAIN_FILE ${CMAKE_TOOLCHAIN_FILE}")
    message(STATUS "CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_ARCHIVE_OUTPUT_DIRECTORY}")
    message(STATUS "CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}")
    message(STATUS "CMAKE_CURRENT_BINARY_DIR ${CMAKE_CURRENT_BINARY_DIR}")


    if(DEFINED TARGET_OS_ANDROID)
        message(STATUS "CMAKE_ANDROID_ARCH_ABI ${CMAKE_ANDROID_ARCH_ABI}")
        message(STATUS "CMAKE_ANDROID_NDK ${CMAKE_ANDROID_NDK}")
        message(STATUS "android sdk directory ${ANDROID_SDK_ROOT}")
    endif()

    message(STATUS "bin directory ${PROJECT_BIN_DIR}")
    message(STATUS "assets directory ${PROJECT_ASSETS_DIR}")
    message(STATUS "static lib directory ${PROJECT_STATIC_LIB_DIR}")
    message(STATUS "package directory ${PROJECT_PACKAGE_DIR}")
    message(STATUS "source root directory ${PROJECT_SRC_ROOT_DIR}")

    message(STATUS "project nlc cpu ${NLC_CPU}")
    message(STATUS "project nlc api ${ANDROID_ABI}")

    message(STATUS "project-nlc Qt6_DIR ${Qt6_DIR}")
    message(STATUS "project-nlc Qt6Core_DIR ${Qt6Core_DIR}")
    message(STATUS "project-nlc Qt6Widgets_DIR ${Qt6Widgets_DIR}")
    message(STATUS "project nlc is defined")

    #=== project subdirectories ===#
    message(STATUS "project nlc adding dependant libs with prefix ${CMAKE_PREFIX_PATH}")

    add_subdirectory(nolimitlib)

    message(STATUS "project nlc dependant libs were added with prefix ${CMAKE_PREFIX_PATH}")

    add_subdirectory(nolimitgui)

    message(STATUS "project nlc applications was added with prefix ${CMAKE_PREFIX_PATH}")


    install(TARGETS nolimitconnect
        RUNTIME DESTINATION "${PROJECT_BIN_DIR}"
        BUNDLE DESTINATION "${PROJECT_PACKAGE_DIR}"
        LIBRARY DESTINATION "${PROJECT_BIN_DIR}"
        ARCHIVE DESTINATION "${PROJECT_STATIC_LIB_DIR}"
    )

    message(STATUS "project nlc end with prefix")
endif() # flatpak build
message(STATUS "Root CMakeLists.txt Done")
